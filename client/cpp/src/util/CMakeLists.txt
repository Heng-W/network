
file(GLOB SRCS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)
add_library(util OBJECT ${SRCS})

file(GLOB INCS RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.h *.hpp)
set(INSTALL_HEAD_FILES ${INCS})

install(FILES ${INSTALL_HEAD_FILES} DESTINATION ${CMAKE_INSTALL_PREFIX}/include/util)
